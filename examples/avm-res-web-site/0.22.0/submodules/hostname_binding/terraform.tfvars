hostname = ""

parent_id = ""

retry = { "error_message_regex" : ["Cannot modify this site because another operation is in progress", "A CNAME record pointing from .* was not found", "A TXT record pointing from asuid\\..* was not found", "Hostname .* does not resolve to the controller IP address", "Validation failed for a hostname"] }

ssl_state = null

thumbprint = null
